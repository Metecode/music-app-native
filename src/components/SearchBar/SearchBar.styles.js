const { StyleSheet } = require("react-native");

export default StyleSheet.create({
    container:{
        backgroundColor:'#eceff1',
        padding:2,
        margin:5,
        borderRadius:5,
    }
})